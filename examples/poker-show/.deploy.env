# poker-show.ru — статика (nginx) + Python API (aiogram + lead receiver)

DOMAIN=poker-show.ru
HEALTH_CHECKS="local:http://127.0.0.1:3002/health public:https://${DOMAIN}/health"
REQUIRE_FILES="api/.env"
